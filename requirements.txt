# ── Dépendances obligatoires ────────────────────────────────────────────────
PySide6>=6.5.0
SQLAlchemy>=2.0.0
python-dateutil>=2.8.0
pywin32>=306; sys_platform=='win32'

# ── Dépendances optionnelles ────────────────────────────────────────────────
# Import de relevés PDF bancaires (Import → PDF)
#   pip install pdfplumber
pdfplumber>=0.11.0

# Correcteur orthographique (notes de transactions)
#   pip install pyspellchecker
pyspellchecker>=0.8.0

# Export PDF du rapport fiscal (Outils → Rapport fiscal → Exporter PDF)
#   pip install reportlab
reportlab>=4.0.0

# ── Développement / tests ───────────────────────────────────────────────────
pytest>=8.0.0
